

services:
  web:
    build: .
    ports:
      - "4010:4010"
    env_file:
      - .env
    volumes:
      - ./instance:/app/instance
    restart: unless-stopped
    environment:
      - FLASK_ENV=production
